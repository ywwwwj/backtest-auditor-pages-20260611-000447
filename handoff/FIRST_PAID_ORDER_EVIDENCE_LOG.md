# First Paid Order Evidence Log

Use this file to record the first real paid Backtest Auditor order.

Goal completion requires evidence of:

```text
1 real payment + 1 delivered or deliverable manual audit
```

Form submissions, verbal interest, or unpaid requests do not complete the goal.

## Order Status

Status:

```text
Not completed yet
```

Update status to one of:

- `Payment requested`
- `Payment received`
- `Audit in progress`
- `Audit delivered`
- `Refunded / invalid`

## Payment Evidence

Date paid:

```text
TBD
```

Buyer name or handle:

```text
TBD
```

Buyer contact:

```text
TBD
```

Amount:

```text
TBD
```

Currency:

```text
TBD
```

Payment method:

```text
TBD
```

Payment proof location:

```text
TBD
```

Acceptable proof examples:

- Stripe / Gumroad / Lemon Squeezy transaction URL
- WeChat / Alipay screenshot path
- bank transfer receipt path
- Netlify `manual-payment-proof` submission containing transaction reference

## Strategy Input

Strategy source:

```text
TBD
```

Input type:

```text
TBD
```

Examples:

- TradingView export
- CSV file
- Python / pandas table
- Excel / Google Sheets copy-paste
- JoinQuant / BigQuant export
- screenshot plus description

Input file or link:

```text
TBD
```

Customer's main question:

```text
TBD
```

## Audit Fulfillment

Workspace used:

```text
app.html
```

Strategy name in app:

```text
TBD
```

Audit date:

```text
TBD
```

Verdict delivered:

```text
TBD
```

Score:

```text
TBD
```

Top fake-strong risk:

```text
TBD
```

Next test recommended:

```text
TBD
```

Paid audit pack file:

```text
TBD
```

Delivery message/location:

```text
TBD
```

## Customer Feedback

Did the review save research time?

```text
TBD
```

Would they use a weekly review workflow?

```text
TBD
```

Would they pay again?

```text
TBD
```

Main pain discovered:

```text
TBD
```

Choose one:

- Import/data preparation
- Cost/slippage assumptions
- Sample size
- OOS / walk-forward proof
- Drawdown/risk mismatch
- Unclear decision process
- Other

Quote or paraphrased feedback:

```text
TBD
```

## Goal Completion Audit

Can the active goal be marked complete?

```text
No
```

Mark `Yes` only if all are true:

- real payment received
- payment proof is recorded above
- strategy input is recorded above
- audit pack is delivered or deliverable
- completion evidence can be inspected later

Reviewer notes:

```text
Awaiting first real payment.
```
